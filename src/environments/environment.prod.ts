const url = 'http://localhost:40328/api/'

export const environment = {
  production: true,
  company: `${url}Company/`,
  companyUser: `${url}CompanyUser/`,
  dataTypes: `${url}IdentificationType/`
}